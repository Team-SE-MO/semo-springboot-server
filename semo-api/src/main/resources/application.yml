spring:
  application:
    name: semo-api

  profiles:
    active: secret

server:
  port: 8080

logging:
  level:
    org.hibernate.type.descriptor.sql: trace
    org.springframework.security: DEBUG
