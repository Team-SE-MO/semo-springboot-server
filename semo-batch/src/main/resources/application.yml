spring:
  application:
    name: semo-batch

  profiles:
    active: secret

  batch:
    jdbc:
      initialize-schema: never
    job:
      enabled: false

server:
  port: 8081

logging:
  level:
    org.hibernate.type.descriptor.sql: trace
