spring:
  application:
    name: semo-batch

  profiles:
    active: secret

server:
  port: 8081

logging:
  level:
    org.hibernate.type.descriptor.sql: trace
    org.springframework.security: DEBUG
